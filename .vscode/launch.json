{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Extension (Clean)",
            "type": "extensionHost",
            "request": "launch",
            "args": [
                "--extensionDevelopmentPath=${workspaceFolder}",
                "--disable-extensions"
            ]
        }
    ]
}